import React from "react";
import { SearchForm } from "components/SearchForm";
import { Layout } from "./styles";

const Home: React.FC = () => (
  <Layout>
    <SearchForm />
  </Layout>
);

export default Home;
